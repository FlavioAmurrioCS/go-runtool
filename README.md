# go-runtool

link-install
gh-install
runtool
  - scrape (get Links from selected page)
  - filter (filter links based on os/arch)
  - link-install (install binary from link)
  - gh-install (based on )
  - run <binary> (loads config file, finds)
